//Write a program to print the sum of the first n odd numbers.
#include <stdio.h>
int  main() {
    int n;
    printf("First n odd numbers: ");
    scanf("%d", &n);

    for (int i = 1; i < n; i+=2) {
        
}
printf("Sum: %d", n*n);
}
